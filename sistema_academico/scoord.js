function disciplina(id, nome, creditos, pre_req) {
    let d = {
        id: id,
        nome: nome,
        creditos: creditos,
        pre_requisitos: pre_req,
    }
    d.getNome() = () {
        return d.nome;
    }

}